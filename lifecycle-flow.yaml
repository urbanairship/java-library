version: 1.1.0
config:
  type: 'java-compile'
  email: 'mib@zillow.com'
source:
  build-job-config:
    default:
      enabled: true
  config:
    type: java-compile
  release-platform-config:
  - branch: compile-for-zillow-1.1.0
    platform: mobile_promote.latest