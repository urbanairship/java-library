
# This isn't fully baked into the build process, it simply provides a
# shortcut for dev environment setup_packages_rst

source "https://rubygems.org"

gem "rake", '~>0.9.6'

group :dev do
  gem 'byebug'
end
